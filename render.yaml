services:
  - type: web
    name: portfolioapp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.19
      - key: SECRET_KEY
        generateValue: true
